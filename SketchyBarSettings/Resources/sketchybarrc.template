# Generated by SketchyBar Settings. DO NOT EDIT BY HAND.
#!/bin/bash

PLUGIN_DIR="$CONFIG_DIR/plugins"

BG_BASE=0xFF1F1F28
BG_SURFACE=0xFF2A2A37
BG_OVERLAY=0xFF363646
FG_PRIMARY=0xFFDCD7BA
FG_SECONDARY=0xFFC8C093
FG_DIM=0xFF727169

PANEL_COLOR=0xD02A2A37
ACCENT=$FG_SECONDARY
TEXT=$FG_PRIMARY
MUTED=$FG_SECONDARY
DIM=$FG_DIM

BAR_MARGIN=0

sketchybar --bar \
  position=top \
  height={{BAR_HEIGHT}} \
  margin=$BAR_MARGIN \
  y_offset={{BAR_Y_OFFSET}} \
  corner_radius={{BAR_CORNER_RADIUS}} \
  blur_radius=0 \
  color={{BAR_BACKGROUND_HEX}} \
  topmost=window \
  sticky=on \
  shadow={{BAR_SHADOW}}

default=(
  padding_left={{BAR_PADDING_LEFT}}
  padding_right={{BAR_PADDING_RIGHT}}
  icon.font="SF Pro Text:Semibold:14.5"
  label.font="SF Pro Text:Medium:13.5"
  icon.color="$TEXT"
  label.color="$TEXT"
  icon.padding_left={{BAR_ICON_PADDING}}
  icon.padding_right={{BAR_ICON_PADDING}}
  label.padding_left={{BAR_ICON_PADDING}}
  label.padding_right={{BAR_ICON_PADDING}}
)
sketchybar --default "${default[@]}"
sketchybar --remove skhd_status 2>/dev/null || true

# SoundSource alias is environment-specific; enable manually if needed.
# SOUNDSOURCE_ALIAS="コントロールセンター,com.rogueamoeba.soundsource(42)"
# if sketchybar --query default_menu_items 2>/dev/null | grep -Fq "$SOUNDSOURCE_ALIAS"; then
#   sketchybar --add alias "$SOUNDSOURCE_ALIAS" center
# fi

{{WIDGET_BLOCKS}}
{{BRACKET_BLOCKS}}
# Initialize all items.
sketchybar --update
